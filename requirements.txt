# HYPERION V9.4 — Dépendances Python
# Installer : pip install -r requirements.txt

# ── IA & LLM ──────────────────────────────────
google-genai==2.0.1

# ── Firebase ──────────────────────────────────
firebase-admin>=6.0.0

# ── Configuration ─────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0

# ── Validation des données ────────────────────
pydantic>=1.10.0,<2.0.0

# ── Requêtes HTTP & Scraping ──────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0            # parsing HTML robuste
aiohttp>=3.9.0         # requêtes async (scraping 10 courses en parallèle)

# ── Extraction PDF (cascade sans Gemini) ──────
pdfplumber>=0.10.0     # extraction texte PDF
pymupdf>=1.23.0        # alternative PDF (fitz)

# ── Calculs numériques ────────────────────────
numpy>=1.24.0
pandas>=2.0.0

# ── Telegram ──────────────────────────────────
python-telegram-bot>=20.0

# ── Tests ─────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
